<%= link_to "" , 
  ownership_path( current_user.haves.find_by(:item_id => item.id) , 
    item_id: item.id, asin: item.asin , type: "Have") ,
  :class => "have-#{item.id || item.asin} btn-have btn btn-warning btn-sm haved",
  :remote => true,
  :method => :delete %> 